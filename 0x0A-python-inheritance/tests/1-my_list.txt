The 'print_sorted' module
-----------------------

Parameters must be type int
Returns a sorted list

First import "print_sorted"
	>>> __import__('1-my_list').MyList

Now you can use it:

Basic usage:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> my_list.print_sorted()
    >>> print(my_list)
    [1, 2, 3, 4, 5]
    [1, 4, 2, 3, 5]

String parameter:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append("Pen")
    >>> my_list.print_sorted()
    >>> print(my_list)
    Traceback(most recent call last):
	...
    TypeError: parameter must be an integer
