// Select element use queryselector with jquery
$(document).ready(function() {
    //Select the header element
    const header = $('header');
    //Modify the color of the header
    header.css('color', '#FF0000');
});